# Dependencies (only used temporarily for local image processing with sharp)
node_modules/
package-lock.json

# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/

# Local Netlify folder
.netlify

# Flagged: watermarked/screenshotted source image, not cleared for use — never commit
assets/_originals/pudim1.png
